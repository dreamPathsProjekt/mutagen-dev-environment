sync:
  development:
    alpha: "./test_folder"
    beta: "stolos.aws:~/test_folder"
    mode: "one-way-safe"
    ignore:
      vcs: true
      # paths:
      #   - "./build/"
      #   - "./node_modules"