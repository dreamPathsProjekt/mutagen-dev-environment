forward:
  defaults:
    socket:
      overwriteMode: "overwrite"
  # remoteDocker:
  #   source: "unix:docker.sock"
  #   destination: "drone.internal:unix:/var/run/docker.sock"
  #   socket:
  #     owner: "root"

sync:
  development:
    alpha: "./test_folder"
    beta: "stolos.aws:~/test_folder"
    mode: "one-way-safe"
    ignore:
      vcs: true
      # paths:
      #   - "./build/"
      #   - "./node_modules"